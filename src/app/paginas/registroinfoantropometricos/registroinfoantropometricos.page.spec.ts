import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RegistroinfoantropometricosPage } from './registroinfoantropometricos.page';

describe('RegistroinfoantropometricosPage', () => {
  let component: RegistroinfoantropometricosPage;
  let fixture: ComponentFixture<RegistroinfoantropometricosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RegistroinfoantropometricosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
