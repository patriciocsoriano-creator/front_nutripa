import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PacienteverpresionPage } from './pacienteverpresion.page';

describe('PacienteverpresionPage', () => {
  let component: PacienteverpresionPage;
  let fixture: ComponentFixture<PacienteverpresionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PacienteverpresionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
