import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MedicoplanalimenticioPage } from './medicoplanalimenticio.page';

describe('MedicoplanalimenticioPage', () => {
  let component: MedicoplanalimenticioPage;
  let fixture: ComponentFixture<MedicoplanalimenticioPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MedicoplanalimenticioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
