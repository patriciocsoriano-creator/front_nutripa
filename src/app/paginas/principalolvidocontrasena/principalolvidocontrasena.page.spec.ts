import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PrincipalolvidocontrasenaPage } from './principalolvidocontrasena.page';

describe('PrincipalolvidocontrasenaPage', () => {
  let component: PrincipalolvidocontrasenaPage;
  let fixture: ComponentFixture<PrincipalolvidocontrasenaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PrincipalolvidocontrasenaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
