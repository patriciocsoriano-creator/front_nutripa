import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MedicoplanalimenticioDetallePage } from './medicoplanalimenticio-detalle.page';

describe('MedicoplanalimenticioDetallePage', () => {
  let component: MedicoplanalimenticioDetallePage;
  let fixture: ComponentFixture<MedicoplanalimenticioDetallePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MedicoplanalimenticioDetallePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
