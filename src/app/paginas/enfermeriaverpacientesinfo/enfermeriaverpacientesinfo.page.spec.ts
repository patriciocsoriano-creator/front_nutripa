import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EnfermeriaverpacientesinfoPage } from './enfermeriaverpacientesinfo.page';

describe('EnfermeriaverpacientesinfoPage', () => {
  let component: EnfermeriaverpacientesinfoPage;
  let fixture: ComponentFixture<EnfermeriaverpacientesinfoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EnfermeriaverpacientesinfoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
