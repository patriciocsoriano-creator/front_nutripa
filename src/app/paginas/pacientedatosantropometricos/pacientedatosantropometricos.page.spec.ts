import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PacientedatosantropometricosPage } from './pacientedatosantropometricos.page';

describe('PacientedatosantropometricosPage', () => {
  let component: PacientedatosantropometricosPage;
  let fixture: ComponentFixture<PacientedatosantropometricosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PacientedatosantropometricosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
