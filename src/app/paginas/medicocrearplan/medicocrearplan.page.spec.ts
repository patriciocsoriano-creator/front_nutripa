import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MedicocrearplanPage } from './medicocrearplan.page';

describe('MedicocrearplanPage', () => {
  let component: MedicocrearplanPage;
  let fixture: ComponentFixture<MedicocrearplanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MedicocrearplanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
